#include "point.h"

//point point::fromXYtoRowCol(point v) {
//	return { (v.y - 1 - TOP) / 2, (v.x - 2 - LEFT) / 4, v.c };
//}
//
//point point::fromRowColtoXY(point s) {
//	return { 4 * s.y + LEFT + 2, 2 * s.x + 1 + TOP, s.c };
//}